namespace HerOClock.Characters
{
    /// <summary>
    /// The four primary attributes, as described in attributes.md.
    ///
    /// The order matters: it is the order used when distributing level points, which keeps
    /// the distribution reproducible.
    /// </summary>
    public enum Attribute
    {
        Power = 0,
        Agility = 1,
        Specialty = 2,
        Constitution = 3
    }
}
